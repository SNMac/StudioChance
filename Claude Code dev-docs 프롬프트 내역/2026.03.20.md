/dev-docs-update
1. 월간 캘린더 선택 날짜 이동시 선택 UI가 이동하는 애니메이션이 있어야 할것 같음. 3일 캘린더를 스크롤하는동안 그냥 딱딱하게 변하는게 좀 부자연스러움.
2. 3일 캘린더 맨 위, 맨 아래 스크롤시 늘어났다가 원래대로 줄어들때까지 스크롤이 안먹힘
3. 오늘 날짜가 가운데에 있음. 제일 왼쪽에 있어야 함
4. 현재 시간 표시 UI 위치가 시간과 안맞는 경우 존재 (예: 2시 2분인데 2시 구분선보다 위에 있음)
5. 현재 시간 표시 UI의 라벨 위치가 3일 캘린더 시간 표시 영역의 시간 라벨과 Y축 정렬이 아직 안되어있음. 현재 시간 표시 라벨(캡슐)의 크기 문제라면 디자인이 유사한 선에서 크기 조정해도 됨. 라벨을 먼저 y축 정렬시키고, 그 다음 캡슐을 씌운다는 느낌
6. 혹시 지금 확대/축소 제스처가 3일 캘린더 일정 표시 영역에서만 적용되어있다면, 시간 표시 영역도 인식할 수 있게 변경
7. 월간 캘린더의 요일 표시 UI 높이는 20
8. 월간 캘린더 UI가 차지하는 높이는 260이고, 그 안에 실질적인 캘린더 부분은 캘린더 UI와 상하좌우 8씩 패딩 존재
9. 월간 캘린더에서 날짜를 선택했을 때, 3일 캘린더에 스크롤되는 애니메이션이 필요
10. 지금 3일 캘린더 시간 표시 영역의 시간 라벨과 일정 표시 영역 구분선의 x축 정렬이 안되어있음. 시간 라벨이 살짝 위에 존재
11. 현재 시간 표시 UI 선이 3일 캘린더 각각 날짜에서 틀어지는 현상 존재
12. 혹시 아이폰 노션 캘린더를 안다면, 그걸 참고로 개발중임.

---

