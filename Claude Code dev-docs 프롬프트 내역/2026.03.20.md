/dev-docs-update
1. 월간 캘린더 선택 날짜 이동시 선택 UI가 이동하는 애니메이션이 있어야 할것 같음. 3일 캘린더를 스크롤하는동안 그냥 딱딱하게 변하는게 좀 부자연스러움.
2. 3일 캘린더 맨 위, 맨 아래 스크롤시 늘어났다가 원래대로 줄어들때까지 스크롤이 안먹힘
3. 오늘 날짜가 가운데에 있음. 제일 왼쪽에 있어야 함
4. 현재 시간 표시 UI 위치가 시간과 안맞는 경우 존재 (예: 2시 2분인데 2시 구분선보다 위에 있음)
5. 현재 시간 표시 UI의 라벨 위치가 3일 캘린더 시간 표시 영역의 시간 라벨과 Y축 정렬이 아직 안되어있음. 현재 시간 표시 라벨(캡슐)의 크기 문제라면 디자인이 유사한 선에서 크기 조정해도 됨. 라벨을 먼저 y축 정렬시키고, 그 다음 캡슐을 씌운다는 느낌
6. 혹시 지금 확대/축소 제스처가 3일 캘린더 일정 표시 영역에서만 적용되어있다면, 시간 표시 영역도 인식할 수 있게 변경
7. 월간 캘린더의 요일 표시 UI 높이는 20
8. 월간 캘린더 UI가 차지하는 높이는 260이고, 그 안에 실질적인 캘린더 부분은 캘린더 UI와 상하좌우 8씩 패딩 존재
9. 월간 캘린더에서 날짜를 선택했을 때, 3일 캘린더에 스크롤되는 애니메이션이 필요
10. 지금 3일 캘린더 시간 표시 영역의 시간 라벨과 일정 표시 영역 구분선의 x축 정렬이 안되어있음. 시간 라벨이 살짝 위에 존재
11. 현재 시간 표시 UI 선이 3일 캘린더 각각 날짜에서 틀어지는 현상 존재
12. 혹시 아이폰 노션 캘린더를 안다면, 그걸 참고로 개발중임.

---

/dev-docs-update
1. 피커를 표시할 때 쓰던 버튼이 피커 표시용이 아니었음. 눌렀을 때 사용자가 멤버로 있는 점포의 목록이 뜨고, 어떤 점포의 일정들을 볼건지 필터링하는 용도(애플 캘린더의 캘린더 선택 버튼과 비슷)
2. 지금 스크롤을 맨 위나 맨 아래로 해서 늘어지는 효과가 뜰 때, 해당 날짜의 스크롤만 늘어지는 효과가 남. 이전처럼 전체 날짜의 스크롤이 늘어져야 함
3. 아직 3일 캘린더의 시간 표시 영역에서 라벨이 구분선과 수평 정렬되어있지 않음.
4. 월간 캘린더 선택 날짜 이동시 선택 UI가 이동하는 애니메이션이 지금 날짜부터 선택한 날짜로 이어지듯이 애니메이션이 있어야 하는데, 선택한 날짜에 먼저 애니메이션이 뜨다가 지금 날짜부터 선택한 날짜로 애니메이션이 이어짐. 그리고 애니메이션으로 뜨는 사각형?에 코너값이 적용되어있지 않음.
5. 지금 예를 들어 현재 날짜 3월에서 작년 12월로 갔다가 오늘 버튼을 누르면 네비바 연/월 UI가 3월로 표시됐다가 1, 2, 3으로 됨. 오늘 버튼뿐만의 문제가 아닌, 캘린더를 선택 날짜로 이동할 때 이런 현상 모두 발생.
6. 3일 캘린더에서 스크롤시 고정되어있는 구분선은 좌측 시간 표시 영역과 일정 표시 영역을 나누는 구분선과, 요일과 종일을 나누는 구분선, 종일과 시간, 일정 영역을 나누는 구분선 뿐. 나머지는 스크롤하면서 날에 맞춰 스크롤되어야함. 날짜별로 갖고있다고 생각하면 좋을듯.
7. 현재 시간 표시 UI의 라벨이 아직 시간 표시 라벨과 수직 정렬되어있지 않음. 좌측으로 더 이동해야함.

---

/dev-docs-update
1. 3일 캘린더 맨 위, 맨 아래 스크롤시 늘어났다가 원래대로 줄어들때까지 스크롤이 안먹힘
2. 점포 필터 버튼(이전 피커 버튼)의 이미지가 변경됨. 이전 이미지로 유지해야 함
3. 현재 시간 표시 라벨이 3일 캘린더 시간 표시 라벨보다 너무 좌측임. 구분선과도 이어지지 않은 상태
4. 지금 월간 캘린더에서 선택한 날짜에서 다른 달로, 예를 들면 12월이 선택되어있는 상태에서 10월 날짜를 선택한다고 하면 네비바 연/월 버튼과 월간 캘린더가 12월로 돌아갔다가 10월로 이동함. 누르자마자 연/월 버튼은 10월로 반영되고, 월간 캘린더는 선택되어있던 날짜보다 선택한 날짜가 이전인 경우, 캘린더 스크롤 없이 월말에서 이어져오는 애니메이션을 적용하고, 만약 이후인 경우 월초에서 이어져오는 애니메이션(현재 선택 날짜 애니메이션)을 적용하면 될듯. 선택되어있던 날짜에서 선택한 날짜로 월간 캘린더가 스크롤되는 애니메이션은 필요 없음.
5. 월간 캘린더 선택 애니메이션에서 날짜를 선택한 경우 해당 날짜에 먼저 애니메이션이 생겼다가 선택되어있던 날짜에서 선택한 날짜로 애니메이션이 생김. 해당 날짜에 먼저 애니메이션이 생기는걸 없앴으면 좋겠음. 2번 깜빡이는 느낌이 듬

---

/dev-docs-update
1. 캘린더 날짜 범위를 안알려줬었네. 2001.01.01 ~ 2100.12.31이야.
2. 내가 직접 3일 캘린더 시간 표시 라벨 위치를 조정해서 현재 시간 라벨이랑 y축 정렬되게 해놨어
3. 현재 시간 라벨과 구분선 사이에 1픽셀정도? 갭이 있어. 이어져있지 않아.
4. 지금은 3일 캘린더를 위아래 스크롤할때 아예 바운싱이 없어졌어. 바운싱은 있되, 바운싱이 멈출때까지 스크롤이 먹히지 않는 현상을 없애줘.
5. 지금 특별한 이유 없이 버튼 대신 GestureDetector를 사용한게 있다면, 버튼으로 교체해줘. CupertinoButton을 기반으로 고쳐줘.
6. 3일 캘린더 날짜 간 구분선 틀어짐이 간혹 발생해. 혹시 하나의 셀 개념이 하나의 날짜라서 그런걸까? 어떻게 고쳐야 할까?

---

/dev-docs-update
1. 지금 스크롤을 맨 위나 맨 아래로 해서 늘어지는 효과가 뜰 때, 해당 날짜의 스크롤만 늘어지는 효과가 남. 계속 반복되고 있는 문제. 전체 날짜의 스크롤이 늘어져야 함
2. 네비바 연월 버튼에 chevron 아이콘이 비율이 깨진 느낌. 만약 깨진거라면, 너비는 지금 상태로 유지한 채로 비율이 깨지지 않도록 변경

---

/dev-docs-update
1. 3일 캘린더 스크롤 바운싱 문제는 해결되었지만, 지금 다시 일정 영역 내부 구분선이 고정되어있는 상태. 3일 캘린더를 좌우로 스크롤할 때 일정 영역 내부 구분선도 날짜에 맞춰서 스크롤되어야 함.
2. 3일 캘린더 영역 좌측 시간 표시 UI와 일정 표시 영역을 구분하는 구분선이 요일 영역을 침범해있음. 이 구분선은 종일 영역부터 시작되어야 함
3. 현재 시간 표시 UI의 라벨 캡슐이 좌측 시간 표시 UI와 일정 표시 영역을 구분하는 구분선에 잘리는 느낌. 사진 첨부함

---

/dev-docs-update
1. '3일 캘린더 영역 좌측 시간 표시 UI와 일정 표시 영역을 구분하는 구분선이 요일 영역을 침범해있음. 이 구분선은 종일 영역부터 시작되어야 함' 이것에 대해 내가 설명을 모호하게 했던것 같아. 종일 영역 안쪽부터 시작된다는 뜻이야. 지금은 종일 영역 바깥부터 시작중이네.
2. 3일 캘린더 요일 표시 안쪽에도 일정 표시 영역 구분선이 이어져있는데, 이것 역시 3일 캘린더 요일 표시 아래부터 시작해야 해. 그러니까, 요일 표시 영역에는 구분선이 없어야 해.
3. 3일 캘린더를 좌우로 스크롤하다보면 아직 스크롤이 어긋나있는 곳이 보여. 예를 들면, 오늘 날짜 버튼을 누른 뒤 스크롤을 살짝 올리고 다음 날짜로 스크롤하면, 다음 날짜는 살짝 올린 스크롤이 반영되어있지 않아.
이제 진짜 거의 다 구현된거같아. 버그만 좀더 잡으면 돼.

---

/dev-docs-update
1. 아직 스크롤이 어긋나있는 버그가 잡히지 않았어.
2. 앱을 시작했을 때 3일 캘린더가 오늘 날짜로 시작하지 않아.

---

/dev-docs-update
1. 아직 스크롤이 어긋나있는 버그가 존재해. 혹시 수정 규모가 커져도 괜찮으니, 어떻게든 이 버그를 수정해줘. 크리티컬한 문제야.
2. 3일 캘린더 시간 표시 영역과 일정 표시 영역을 나누는 구분선이 가려져서 안보이는 느낌이야.
3. 현재 시간 표시 캡슐의 Positioned.right를 내가 조정했어. 이에 맞춰서 현재 시간 표시 구분선도 조정해줘.

---

/dev-docs-update
나머지 문제들은 해결됐어. 근데 아직 3일 캘린더에서 각 날자의 스크롤이 어긋나있는 버그가 존재해.

---

/dev-docs-update
아직 3일 캘린더에서 각 날짜의 스크롤이 어긋나있는 버그가 존재해. 근본적인 문제의 원인 파악을 하고, 그 다음 해결 방법을 찾아줘.

---

/dev-docs-update
거의 수정됐어. 이제 현재 보이는 3일 중에서 스크롤이 어긋나있는 날짜가 보이는 버그는 없는데, 스크롤이 끝나고 날짜 단위로 스크롤이 붙을 때?(페이징처럼) 그때 다음 날짜(날짜 단위로 스크롤이 붙으면 안보이는 쪽)가 어긋나있는게 보여. 이걸 수정할 계획을 짜고 바로 수정해줘.
