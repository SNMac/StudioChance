/dev-docs-update
오버플로우 셀을 탭했을 때 뜨는 이벤트 목록 모달에서, 모달 배경이 systemGroupedBackground색이고, 내부 이벤트 리스트는 GroupedFormContainer 안에서 표시되면 좋겠어.
또, 예전에 colors.dart에 예약 관련 색을 지정했었는데, 알고보니 store_color.dart 안에 enum에 색상 관련 코드를 작성했었더라고. 이 enum으로 StoreColor 관련 코드를 통합해줘.

---

/dev-docs-update
이벤트 목록 모달에서 이벤트에 지금은 라벨 색상, 이름, 시간만 뜨는데, 이름 옆에 인원 수도 뜨게 해줘. 예약 셀처럼 말야. 그리고 박지원, 최수아, 김민준, 이서준 이 4개 이벤트는 시작 시간이 같은 오버플로우 이벤트인데, 만약 시작 시간이 다르지만 중간에 4개가 겹치는 경우는 어떻게 돼? 이것도 목데이터를 만들어줘.
또, 이벤트 목록 모달과 이벤트 세부 모달에서 appBar로 CustomAppBar를 사용하도록 해줘. 이벤트 목록 모달의 경우 '예약 목록'이 제목이고, 이벤트 세부 모달에선 '예약 정보'로 해줘. 이벤트 목록 모달에선 appBar에 별도 버튼은 없어. 이벤트 세부 모달에선 appBar 내부 우측에 '편집' 버튼을 만들어줘. 이벤트 목록 모달에서 GroupedFormContainer는 safeAreaWithPadding 안에 들어있게 해줘.

---

/dev-docs-update
appBar의 우측 버튼은 AppBarActionButton을 사용해줘.
그리고 예약 목록 모달에서 appBar 위에 systemGroupedBackground색이 살짝 보여. StoreAddressInputScreen의 모달에선 그러지 않았거든. grabber가 문제인거야? 혹시 그렇다면 grabber가 존재하는 영역은 appBar 배경색으로 채워도 돼. appBar 아래만 systemGroupedBackground색으로 유지되면 돼.

---

/dev-docs-update
예약 목록 모달과 예약 세부 모달에서 appBar의 배경색은 모달 배경색과 동일하게 보이도록 수정해줘(systemGroupedBackground 혹은 clear). 그리고 appBar는 모달 상단과 14정도 떨어져 있어야 해.
예약 세부 모달에선 appBar 내부 좌측에 '취소' 버튼이 있어야 해.

---

/dev-docs-update
안드로이드에서 이벤트 세부 모달을 볼 때, '취소' 버튼과 '편집' 버튼을 눌렀을 때 눌림 효과가 나는 크기가 달라. '편집' 버튼과 동일한 크기를 갖도록 해줘. 이렇게 변경했을 때 iOS 버전에서 side effect가 일어날 것 같다면 말해줘.

---

/dev-docs-update
아직 안드로이드에서 '취소' 버튼을 눌렀을 때 보이는 눌림 효과가 '편집' 버튼을 눌렀을 때보다 너무 커. 그리고 '취소' 버튼이 너무 우측으로 치우쳤어. '편집' 버튼의 우측 여백과 '취소'버튼의 좌측 여백이 같아야 해. (일반적인 뒤로 가기 버튼에 side effect 미치지 않도록 수정 필요)

---

/dev-docs-update
아직 그대로야. 그냥 뒤로 가기 버튼을 커스텀 할 땐 AppBarActionButton을 쓰도록 하자. 단, regular weight를 쓸 것인지, semibold를 쓸 것인지를 bool로 입력받게 해서 써야겠어. AppBarModalBackButton에서 수정했던 사항은 롤백해줘. 이건 일반적인 뒤로가기 버튼으로만 쓸거야. 모달 내부 '취소' 버튼은 titleLarge 크기에 regular weight를 써야 해.

---

/dev-docs-update
예약 목록 모달과 예약 세부 모달의 grabber를 컴포넌트로 빼줘.
