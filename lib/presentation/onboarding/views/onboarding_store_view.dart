import 'package:flutter/material.dart';

class OnboardingStoreView extends StatelessWidget {
  const OnboardingStoreView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
